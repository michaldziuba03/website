import React, { FC } from 'react';
import { Section } from '../../components/section/Section';

export const Hero: FC<{}> = () => {
    return (
        <Section>
            <h1>Hello world.</h1>
        </Section>
    )
} 
