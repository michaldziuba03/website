---
import { Image } from 'astro:assets';

import { skills, SkillIcon } from './SkillIcon';
import me from '../assets/me.png';
import { Container } from './Container';
import { SectionHeading } from './SectionHeading';

---
<section id="about" class="w-full py-16 sm:py-20 md:py-32">
  <Container className="w-full max-w-screen-xl">
    <div class="flex flex-col-reverse md:flex-row gap-8 sm:gap-12 md:gap-16 lg:gap-8">
      <div class="md:w-1/2 lg:w-3/5">
        <SectionHeading level="h1">
          About
        </SectionHeading>
        <p class="text-slate-700 leading-relaxed sm:leading-loose text-base max-w-2xl mt-4">
          I'm Michał, a software developer from Poland. I'm all about backend development and tinkering with operating systems since 2018. I enjoy creating and contributing to open source software, as I believe in the power of collaboration and sharing knowledge.  Besides programming, I like metal music, video games, history, and dogs.
        </p>

        <h2 class="font-black text-xl sm:text-2xl mt-12 sm:mt-16">Tech stack</h2>
        <p class="text-slate-700 text-sm sm:text-base max-w-2xl mt-2">
          Some technologies and tools I use on daily basis.
        </p>
        
        <div class="flex gap-2 sm:gap-3 flex-wrap mt-4 sm:mt-5">
          {skills.map((name) => (<div class="flex gap-2 sm:gap-3 justify-center items-center border border-gray-300 bg-white py-1.5 px-3 sm:px-4 font-semibold text-xs sm:text-sm rounded">
            <SkillIcon skill={name} className="w-4 h-4 sm:w-5 sm:h-5 text-gray-600" />
            {name}
          </div>))}
        </div>

      </div>
      
      <!-- Animated profile image from hero -->
      <div class="md:w-1/2 lg:w-2/5 flex justify-center md:justify-end">
      <div class="relative">
        <div class="relative group">
          <!-- Floating elements -->
          <div class="absolute -top-2 sm:-top-4 -left-2 sm:-left-4 w-8 h-8 sm:w-12 md:w-16 sm:h-12 md:h-16 bg-primary/20 rounded-full animate-pulse"></div>
          <div class="absolute -bottom-3 sm:-bottom-6 -right-3 sm:-right-6 w-6 h-6 sm:w-8 md:w-12 sm:h-8 md:h-12 bg-blue-500/20 rounded-full animate-bounce delay-300"></div>
          <div class="absolute top-1/2 -left-4 sm:-left-8 w-4 h-4 sm:w-6 md:w-8 sm:h-6 md:h-8 bg-green-500/20 rounded-full animate-ping delay-700"></div>
          
          <!-- Main profile image -->
          <div class="relative overflow-hidden rounded-2xl bg-gradient-to-br from-primary/10 to-blue-500/10 p-3 sm:p-4 backdrop-blur-sm border border-white/20 shadow-2xl group-hover:scale-105 transition-transform duration-300">
            <Image
              src={me}
              alt="Me"
              class="w-64 h-64 sm:w-80 sm:h-80 md:w-96 md:h-96 object-cover rounded-xl shadow-lg"
            />
            
            <!-- Overlay gradient -->
            <div class="absolute inset-3 sm:inset-4 rounded-xl bg-gradient-to-t from-primary/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
          </div>
          
          <!-- Code snippets floating around -->
          <div class="absolute -top-4 sm:-top-8 right-2 sm:right-4 bg-gray-900 text-green-400 px-2 sm:px-3 py-1 sm:py-2 rounded-lg text-xs sm:text-sm font-mono shadow-lg animate-float">
            <span class="text-gray-500">$</span> npm start
          </div>
          
          <div class="absolute bottom-2 sm:bottom-4 -left-6 sm:-left-12 bg-gray-900 text-blue-400 px-2 sm:px-3 py-1 sm:py-2 rounded-lg text-xs sm:text-sm font-mono shadow-lg animate-float delay-500">
            <span class="text-purple-400">const</span> <span class="text-yellow-400">dev</span> = true
          </div>
        </div>
      </div>
      </div>
    </div>
  </Container>
</section>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }
  
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
</style>
